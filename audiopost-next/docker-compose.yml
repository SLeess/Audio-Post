version: '3.8'
services:
  next-app:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install react-hook-form @hookform/resolvers zod && npm run dev"